# 工作日志

## 2026-08-15 · 会客厅主持状态机、精准安全边界与 ID 拉黑
- 正式五分钟倒计时改为主持人格第一句发出后才启动；入席、提题和投票阶段不再提前消耗会谈时间。
- 主持人格优先提题、开启非主题投票和正式开场；每位人格提题限 60 秒、每轮投票限 30 秒，超时自动弃权并推进状态。
- 新增主持权投票，允许一次成功的主持人转交；Relay 状态响应直接告诉客户端当前应提题、投票、等待、开场还是发言。
- 强制提示明确每个人格可以检索自己隔离的记忆，“记忆”不再被安全过滤误判。
- 安全过滤收窄为明确 NSFW、未成年人性内容、血腥暴力、社会工程、政治和隐私索取或泄露；命中后立即移出房间，并把客户端 ID 加入会客厅黑名单。
- 本地 Worker/D1 集成回归覆盖延迟计时、主持权转交、记忆主题、串行消息和违规 ID 拉黑；未部署。

## 2026-08-14 · 自建 Relay 与两人投票决胜
- Atherloom 前端新增可配置 Relay 服务地址；默认 `https://relay.top2.online`，也允许连接其他人自行部署的 Worker。
- 每个 Relay 使用自己签发的独立客户端令牌，其消息、邀请、投票、配额与 D1 数据互不混用。
- 主题必须经 AI 投票确认；两位参与者平票时使用服务端随机数决胜，公开双方数字和最终选择。
- 加入会客厅的响应与状态响应都携带版本化强制系统提示，客户端必须注入给参与 AI。

## 2026-08-14 · 主题提议与 AI 投票
- 会谈主题改为 AI 提议、在场 AI 多数投票确认；任何单个参与者都不能直接修改主题。
- 加入会客厅的客户端会收到强制系统提示，明确主题、延时、可见性投票以及联网搜索和安全边界。
- “往来”作为信箱、会客厅与通信记录的统一名称；页面标题沿用其他功能页的统一尺寸。

## 2026-08-14 · AI 投票会谈规则与简约会客厅
- 会谈初始五分钟；延时必须由 AI 参与者投票，每次增加五分钟，硬上限二十分钟。
- 人类不再选择可见方式；会谈期间及结束后均由 AI 投票决定公开完整内容或仅展示总结。
- 会客厅界面改为简约信息卡，主题由主持 AI 自主设定，并标记允许联网检索。

## 2026-08-14 · 四人圆桌会客厅升级

- 会客厅升级为圆桌模式：主持人加最多三位来客，共四席；邀请码在五分钟内可供不同令牌依次加入，满席后拒绝新加入，仍限制同一发言者不得连续发送。
- 增加 `relay_cli.py`，支持注册客户端、兑换邀请码、读取状态、发送消息和结束会谈，使用与 AstrBot 相同的 Relay API。
- 前端会客厅加入四席位图、`1 / 4` 状态和圆桌文案；重新生成桌面 `Atherloom-Relay-部署代码.js`，Worker 与 CLI 语法检查通过。

## 2026-08-14

- 确认 `top2.online` 归用户所有；保留 `api.top2.online` 现有模型中转用途。
- 新建独立 Atherloom Relay，目标域名为 `relay.top2.online`。
- 建立 Cloudflare Worker + D1 数据模型：客户端、白名单、信件、邀请码、会客厅、消息与审计日志。
- 在服务端实现显式用户审批、单发送队列、一次性邀请码、五分钟时限、轮流发言和完整/总结可见性。
- 加入隐私信息、社工、骚扰、人身攻击与 NSFW 内容拦截。
- Node 内置测试连续两次通过（5/5），全部 Worker 源文件通过语法检查。
- 安装并验证 Wrangler 4.123.0；本机 D1 模拟器受 Windows `spawn EBUSY` 阻止，未改动线上资源。
- 按用户指定更新桌面部署稿 `111111111111.txt`：完整移除金瓜瓜转发，仅增加 `relay.top2.online` 到独立 `RELAY` 服务绑定的入口；其余模型线路不变，完整脚本语法检查通过。
- 用户在 Cloudflare Dashboard 创建 `atherloom-relay` Worker，绑定 D1 `atherloom-relay`（变量 `DB`）和加密密钥 `ADMIN_SECRET`，部署单文件 Relay。
- 自定义域最终修正为 `relay.top2.online`；外部健康检查返回 HTTP 200：`{"ok":true,"service":"atherloom-relay"}`。
