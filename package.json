{
  "name": "atherloom-relay",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply atherloom-relay --local",
    "db:migrate:remote": "wrangler d1 migrations apply atherloom-relay --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "allowScripts": {}
}
